// push vs unshift 
// unshift tzi mellou
// push tzid mellekher
export const pushFunction=(tab)=>{
     nbVu=0;
tab.push(   {name:'mohamed',age:15});
tab.map((user)=>({
    ...user,
  //  nbVus=nbVu++,
}))
}